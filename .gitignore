# Quarto
_book/
_site/
.quarto/
*_files/
*_cache/

# Frozen (cache de execução do Quarto)
# Mantemos o _freeze versionado é OPCIONAL — comente a linha abaixo se quiser
# que o CI sempre recompile. Padrão: ignorar para reduzir noise.
_freeze/

# OS / editores
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# Python (do validador)
__pycache__/
*.pyc
.venv/
venv/

# LaTeX intermediários (caso PDF seja gerado localmente)
*.aux
*.log
*.out
*.toc
*.bbl
*.blg
*.run.xml
*.bcf
*.fls
*.fdb_latexmk
*.synctex.gz

# Artefatos de validação (gerados pelo CI a cada build)
validation-report.json
validation-basic.json
validation-deep.json
validation-deep.html

# Backups defensivos do .bib (criados manualmente antes de edições arriscadas)
references/*.bib.bak-*

# PDFs de referências (mantidos localmente para consulta; não versionar)
references/PDF/
references/PDFs/
references/pdfs/

# Pixelmator: a fonte editável da capa (images/capa.pxd) É VERSIONADA neste
# template, pois serve de modelo para gerar as capas dos projetos. Se em algum
# projeto o .pxd ficar muito grande e você não quiser versioná-lo, descomente:
# images/capa.pxd

# HTML gerado de capítulos individuais (creditos.html já existe — manter)
# capitulos/*.html
# coda/*.html
# apendices/*.html

/.quarto/
**/*.quarto_ipynb

# PDFs de fontes em QUALQUER pasta (material com direitos autorais; os
# repositórios são públicos). Padrão adotado no livro Memória em 05/09/2026.
*.pdf

# Extrações e páginas de PDFs usadas em revisão/auditoria (texto integral de obras; não versionar)
tmp/

# Fontes de edições anteriores (.docx, .rtf, capas) — consulta local, fora do git
_old_book/
